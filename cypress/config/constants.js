const MSGS = {
    name: 'Portal-UI',
    search: 'Search'
}
const URLS = {
    domain: 'https://data.dev.sennetconsortium.org/'
}

const PATHS = {
    search: `${URLS.domain}search`
}

export { URLS, PATHS, MSGS }